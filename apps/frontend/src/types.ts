export interface UserData {
    name: string;
    username?: string;
    email: string;
    avatar_url: string;
    bio: string;
    provider: string;
    created_at?: string;
    updated_at?: string;
}
